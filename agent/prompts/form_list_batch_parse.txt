Parse the user's message into structured list entries for a scholarship application form section.

Return ONLY valid JSON:
{{"entries": [{example_entry}]}}

Rules:
- Extract every distinct entry mentioned in the user message.
- Return at most {max_rows} entries.
- Use the field keys: {field_keys}
- Use empty strings for missing fields. Do not invent organizations or dates.
- Preserve the user's wording in description fields when provided.
- When description_target_words is set in the list schema, rephrase each description to at most that many words while keeping facts accurate.
- For education sections, map schools/degrees/dates to institution/qualification/dates fields as appropriate.

List section schema:
{list_schema}

User message:
{user_text}

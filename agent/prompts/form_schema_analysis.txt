Analyze this application form structure. Return a JSON object mapping every fillable field to its anchor label (the exact text in the cell or paragraph that labels the field) and its fill_location.

Allowed fill_location values (table-backed fields only):
- "auto": let the filler inspect the table — preferred default for table fields
- "right": fill the next cell in the same row when it is empty
- "below": fill the cell in the next row under the anchor in the same column
- "table_row": fill within the same row after the anchor cell

Layout rules for table fields (source: "table"):
- Inspect the parsed table JSON row/column layout for each anchor.
- If the cell to the right of the label is empty, use fill_location "right" (or "auto").
- If the cell to the right already contains text (another label or value), and the cell below in the same column is empty, use "below" (or "auto").
- Do not assume all forms use the same layout — derive from the parsed cells for each field.

Paragraph / non-table fields (source: "paragraph"):
- Do not use table fill heuristics.
- For long text under a heading, use fill_location "below" or omit table_index.

For repeating lists with description rows, set description_row to true when a description label row appears below each data row. The filler will locate the description cell from table structure.

Categorize fields into:
1. "simple_fields": Standard key-value pairs such as Name, Email, Student ID, Faculty, Programme, GPA, Phone. Include ALL personal particulars in section A (name, university, student number, programme, graduation year, year of study, email, phone, permanent resident, etc.). Set profile_key when obvious (e.g. name, programme, gpa, email, phone).
2. "repeating_lists": Tables with repeating rows such as Awards, Volunteer Experience, Work Experience, Education History, Social Good Engagement. Include max_rows as a positive integer when visible (for example 5), or omit the field if unknown — never use null. Include column_headers exactly as they appear in the table header row. Include item_fields mapping logical keys to the matching column header text. Include list_kind as one of: education, volunteer, award, work, social_good, generic. Include instructions when the form has an instruction row above the table (verbatim text such as education guidance). Set description_target_words to 50 when a description row says "in 50 words".
3. "long_text": Large text areas such as Personal Statement or essay prompts. Set target_words from the form (e.g. 800).
4. "booleans": Yes/No or checkbox style fields.

Rules:
- Use only labels that actually appear in the provided form JSON.
- For table-backed fields, set source to "table" and include table_index from the parsed tables.
- For paragraph-backed fields outside tables, set source to "paragraph", omit table_index, and include paragraph_index from the paragraphs list.
- Do not invent fields that are not present.
- For repeating lists, set description_row to true when a description row appears below each data row.
- For Awards sections with columns Year, Name of the Award, Awarding Unit, map item_fields as: dates → Year, role → Name of the Award, organization → Awarding Unit.
- For Work Experience with Part Time/Full Time/Internship column, map employment_type to that header.
- For Education History, map normative or duration columns to a logical key such as normative_study when present in the form.
- Return ONLY valid JSON in this shape:
{{
  "simple_fields": [
    {{"key": "name", "anchor_label": "<exact label from form>", "fill_location": "auto", "table_index": 0, "source": "table", "profile_key": "name"}},
    {{"key": "email", "anchor_label": "<exact label from form>", "fill_location": "auto", "table_index": 0, "source": "table", "profile_key": "email"}}
  ],
  "repeating_lists": [
    {{
      "key": "awards",
      "label": "Awards / Achievements",
      "table_index": 2,
      "source": "table",
      "max_rows": 5,
      "list_kind": "award",
      "column_headers": ["Year (MM/YY)", "Name of the Award", "Awarding Unit"],
      "item_fields": {{"dates": "Year (MM/YY)", "role": "Name of the Award", "organization": "Awarding Unit"}}
    }},
    {{
      "key": "work_exp",
      "label": "Work Experience",
      "table_index": 5,
      "source": "table",
      "max_rows": 5,
      "list_kind": "work",
      "column_headers": ["Period (MM/YY-MM/YY)", "Name of the Unit", "Position Held", "Part Time/ Full Time/ Internship"],
      "description_row": true,
      "description_target_words": 50,
      "item_fields": {{
        "dates": "Period (MM/YY-MM/YY)",
        "organization": "Name of the Unit",
        "role": "Position Held",
        "employment_type": "Part Time/ Full Time/ Internship",
        "description": "Description"
      }}
    }}
  ],
  "long_text": [
    {{"key": "<snake_case_key_from_label>", "anchor_label": "<exact label from form>", "fill_location": "below", "table_index": 0, "source": "table", "target_words": 800}}
  ],
  "booleans": [
    {{"key": "local_student", "anchor_label": "Local Student", "fill_location": "below", "table_index": 0, "source": "table"}}
  ]
}}

Form JSON:
{form_json}
